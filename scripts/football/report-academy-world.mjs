import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import {gunzipSync} from 'node:zlib';
import {createHash} from 'node:crypto';
import {leagueSystems} from '../../src/competitions/catalog.js';
import {registeredPlayers,playerAgeOnDate} from '../../src/competitions/registry.js';

const root=fileURLToPath(new URL('../../',import.meta.url));
const read=file=>JSON.parse(fs.readFileSync(path.join(root,file),'utf8'));
const world=read('artifacts/academy-world/world-final/report.json'),career=read('artifacts/academy-world/career-final/report.json'),parity=read('artifacts/academy-world/parity-optimized/parity.json');
const regressionPath='artifacts/academy-world/goalkeeper-regression/report.json';
const regression=fs.existsSync(path.join(root,regressionPath))?read(regressionPath):null;
const regressionSection=regression?`### 门将保护修复后的独立回归\n\n该20季数据保留修复前固定版本；其暴露的AI外租/释放第二门将问题已单独修复，玩家手动操作仍保留原有最低一门将规则。修复后从同一个329年末快照分支，330单季回归状态为 **${regression.status}**，真实正式比赛${regression.matches}场、全世界可用性告警${regression.availabilityWarnings.length}次、引擎中断${regression.engineFailures.length}次。\n\n目标俱乐部半年度评估前后门将数：${regression.juneReview.map(r=>`${r.date} ${r.goalkeepers}人`).join(' → ')}。原告警两天可用门将：${regression.targetAugustFixtures.map(m=>`${m.date} ${m.availableGoalkeepers}人`).join('；')}。新版本源指纹\`${regression.sourceHash}\`；父快照完整season SHA\`${regression.parent.seasonHash}\`。\n\n[单季回归完整证据](../../${regressionPath})及[摘要](../../artifacts/academy-world/goalkeeper-regression/report.md)。**修复后单季回归不能代替新版20季验证。**\n`:'### 门将保护修复回归\n\n本次20季仍为固定基线；独立修复回归结果尚未生成。\n';
if(world.status!=='completed'||world.seasons.length!==20||career.status!=='completed'||career.seasons.length!==3)throw Error('最终20季世界与3季经理尚未完成，不能生成完成报告');
if(world.sourceHash!==career.sourceHash||!parity.equal)throw Error('来源版本或完整赛季保真证据不一致');
const diagnosis=read('artifacts/academy-world/availability-diagnosis.json');
if(diagnosis.sourceModelHash!==world.sourceHash||!diagnosis.complete||diagnosis.cases.length!==world.availabilityWarnings.length)throw Error('告警诊断必须覆盖同源最终20季');
const warningRows=Object.entries(diagnosis.counts).map(([reason,count])=>`| ${reason} | ${count} |`).join('\n');
const last=world.seasons.at(-1),initial=world.baseline,firstYear=world.seasons[0].year,finalYear=last.year;
const f=(x,n=2)=>Number.isFinite(x)?x.toFixed(n):'—';
const mean=a=>a.length?a.reduce((n,x)=>n+x,0)/a.length:0;
const total=(a,key)=>a.reduce((n,x)=>n+x[key],0);
const latest=JSON.parse(gunzipSync(fs.readFileSync(path.join(root,'artifacts/academy-world/world-final/checkpoint.json.gz')))).season;
function capacity(s){
 const teams=new Map(Object.values(s.members).flat().map(id=>[id,{id,total:0,adult:0}]));
 for(const p of registeredPlayers(s)){
  if(!p.club||p.registrationStatus&&!['senior','loan'].includes(p.registrationStatus))continue;
  const team=teams.get(p.club);team.total++;team.adult+=Number(playerAgeOnDate(p,s.date)>=21);
 }
 const divisions=new Map(leagueSystems.flatMap(system=>system.levels).map(d=>[d.id,d]));
 const clubDivision=new Map(Object.entries(s.members).flatMap(([id,clubs])=>clubs.map(club=>[club,id])));
 const generationMinutes=Object.fromEntries(['closed','openFirst','openSecond','openThird'].map(key=>[key,{total:0,newGeneration:0,share:null}]));
 for(const fixture of s.fixtures){
  if(!fixture.report)continue;
  for(let side=0;side<2;side++){
   const division=clubDivision.get(side?fixture.away:fixture.home),tier=divisions.get(division)?.tier;
   const key=division==='closed'?'closed':tier===1?'openFirst':tier===2?'openSecond':'openThird',group=generationMinutes[key];
   for(const line of fixture.report.players[side]){group.total+=line.minutes;if(s.playerRegistry.players[line.id])group.newGeneration+=line.minutes;}
  }
 }
 for(const group of Object.values(generationMinutes))group.share=group.total?group.newGeneration/group.total:null;
 const rows=[...teams.values()];return {year:s.year,above40:rows.filter(t=>t.total>40),above25Adults:rows.filter(t=>t.adult>25),maxTotal:Math.max(...rows.map(t=>t.total)),maxAdults:Math.max(...rows.map(t=>t.adult)),generationMinutes};
}
const capacityByYear=world.seasons.map(s=>capacity(s.year===last.year?latest:JSON.parse(gunzipSync(fs.readFileSync(path.join(root,`artifacts/academy-world/world-final/season-${s.year}.json.gz`)))).season));
const capacityRows=capacityByYear.map(y=>`| ${y.year} | ${y.above25Adults.length} | ${y.above40.length} | ${y.maxAdults} | ${y.maxTotal} |`).join('\n');
const generationRows=capacityByYear.map(y=>`| ${y.year} | ${['closed','openFirst','openSecond','openThird'].map(key=>`${f(y.generationMinutes[key].share*100,1)}%`).join(' | ')} |`).join('\n');
const cohorts=last.cohorts,firstCohort=cohorts[firstYear];
const finalSourceHash=Object.fromEntries(Object.entries(world.sourceHashes).map(([file,hash])=>[file,fs.existsSync(path.join(root,file))?createHash('sha256').update(fs.readFileSync(path.join(root,file))).digest('hex')===hash:false]));
const differingFiles=Object.entries(finalSourceHash).filter(([,same])=>!same).map(([file])=>file);
const shallow=last.teams.filter(t=>t.depthShortage),hard=last.teams.filter(t=>t.hardShortage),noBacks=last.teams.filter(t=>!t.positions.LB||!t.positions.RB);
const firstDepthYear=world.seasons.find(s=>s.teams.some(t=>t.depthShortage));
const firstDepthExamples=firstDepthYear?.teams.filter(t=>t.depthShortage).map(t=>`${t.id}：总${t.players}人，GK ${t.positions.GK}、CB ${t.positions.CB}、RW ${t.positions.RW}、ST ${t.positions.ST}`).join('；')||'未发现';
const leagueRows=leagueSystems.map(system=>{
 const columns=system.levels.map(d=>d.tier===1?'一级':`${d.tier}级`);
 const rows=[{year:'初始',divisions:initial},...world.seasons].map(s=>`| ${s.year} | ${system.levels.map(d=>f(s.divisions[d.id].startingCA.mean)).join(' | ')} |`).join('\n');
 return `### ${system.name}\n\n| 年份 | ${columns.join(' | ')} |\n|---|${columns.map(()=>'---:').join('|')}|\n${rows}`;
}).join('\n\n');
const driftRows=leagueSystems.flatMap(system=>system.levels.map(d=>{
 const a=initial[d.id].startingCA.mean,b=last.divisions[d.id].startingCA.mean;
 return `| ${d.name} | ${f(a)} | ${f(b)} | ${f(b-a)} | ${last.divisions[d.id].hardShortage.length} | ${last.divisions[d.id].depthShortage.length} |`;
})).join('\n');
const annualRows=world.seasons.map(s=>`| ${s.year} | ${s.matches} | ${s.retired} | ${s.outcomes.senior+s.outcomes.loan} | ${s.outcomes.free} | ${s.firstProfessionalAppearances.ages.count} | ${f(s.firstProfessionalAppearances.ages.mean)} | ${s.teams.filter(t=>t.depthShortage).length} | ${f(s.saveBytes/1048576,1)} |`).join('\n');
const careerRows=career.seasons.at(-1).tracked.map(p=>{
 const start=career.trackedInitial.find(t=>t.id===p.id);
 return `| ${p.role} | ${p.name} | ${start.age} | ${f(start.ability)}→${f(p.ability)} | ${f(p.minutes,0)} | ${p.youthMinutes} | ${p.status} | ${p.observation.forecastLow}–${p.observation.forecastHigh} |`;
}).join('\n');
const positionRows=Object.keys(last.teams[0].positions).map(position=>`| ${position} | ${last.teams.reduce((n,t)=>n+t.positions[position],0)} | ${last.teams.filter(t=>t.positions[position]===0).length} |`).join('\n');
const cohortRows=Object.entries(cohorts).map(([year,c])=>`| ${year} | ${c.count} | ${c.everProfessional} | ${f(c.everProfessional/c.count*100,1)}% | ${c.outcomes.youth} | ${c.outcomes.senior+c.outcomes.loan} | ${c.outcomes.free} | ${c.outcomes.retired} |`).join('\n');
const drifts=Object.entries(last.divisions).map(([id,d])=>({id,drift:d.startingCADrift})).sort((a,b)=>a.drift-b.drift);
const tierInversions=leagueSystems.filter(s=>s.levels.length>1).flatMap(s=>s.levels.slice(1).filter((d,i)=>last.divisions[s.levels[i].id].startingCA.mean<=last.divisions[d.id].startingCA.mean).map(d=>`${s.name}${d.tier-1}级≤${d.tier}级`));
const severeDrift=drifts.filter(d=>Math.abs(d.drift)>=5);
const conclusion=hard.length||world.engineFailures.length?'存在比赛运行或名单硬缺口，不能视为长期运行通过。':severeDrift.length||tierInversions.length?`正式比赛能够持续运行，但不能视为长期平衡完成：${severeDrift.length}个级别相对初始首发能力变化达到5分，另有位置深度和出场机会问题，需要继续校准。`:'正式比赛与基本名单供需保持可运行；本次未发现5分以上级别均值漂移或相邻级别倒挂。仍需多组世界与玩家策略验证。';
const minutesAll=total(world.seasons.flatMap(s=>Object.values(s.u21)),'minutes');
const under21Final=Object.entries(last.u21).map(([id,v])=>`| ${leagueSystems.flatMap(s=>s.levels).find(d=>d.id===id).name} | ${v.players.length} | ${f(v.minutes,0)} | ${v.appearances} |`).join('\n');
const deferred=Object.values(latest.playerRegistry.registrations).filter(r=>r.status!=='retired'&&r.retirementDeferredYear).length;
const noHealthyGoalkeeper=world.availabilityWarnings.filter(w=>w.goalkeepers===0),belowEleven=world.availabilityWarnings.filter(w=>w.available<11);
const warningExamples=noHealthyGoalkeeper.slice(0,3).map(w=>`${w.date} ${w.clubId}（可用${w.available}人，自然门将0人）`).join('；');
const doc=`# 青训长期验证\n\n${conclusion}\n\n本报告的20季数据为**门将深度修复前的固定版本基线**，修复后330单季回归单列，未混合数据。\n\n本报告记录 ${firstYear}–${finalYear} 年 **20 个完整赛季、${world.matches.toLocaleString('en-US')} 场真实正式比赛**，以及独立的三个赛季经理情景 **${career.matches.toLocaleString('en-US')} 场正式比赛、${career.coachedMatches} 场经理入口执教**。没有替换成抽签比分或补写职业分钟。\n\n## 口径与复现\n\n- 每年268家俱乐部各加入3名可追踪代表青年，共804人；三人代表学院可追踪名单，青年队其余位置由稳定的背景队友参加竞争。这不是全人口职业选拔率。\n- 正式比赛走生产引擎；青年比赛沿用按周排阵与分钟分配，不是完整青年比赛引擎。\n- 平均首发CA用当期成长后的属性，以4-3-3选11人并按所在槽位评价。每队先计算首发11人的均值，再对同级俱乐部等权平均。结构比较暂时忽略伤停；比赛当日可用性另行记录。不是全队平均能力，也不直接等于比赛胜率。\n- 首次成年正式出场年龄只统计本次生成青年；初始职业球员没有之前赛季履历。U21分钟按每场比赛当天年龄计算，包含联赛与杯赛。\n- 世界全程使用默认0.6训练负荷，经理技术训练也为0.6。高负荷杯赛减量与恢复选择依专项单元测试验证，本次长跑不是三种负荷的随机对照实验。\n- 只有一个固定世界和一套经理政策，不是多随机种子蒙特卡洛研究，也不证明模型已拟合现实足球。初始职业球员保留旧成长逻辑，新生代采用属性包络；不同年龄与不同生成机制混合，不能把群体CA差值直接当作潜力参数误差。\n\n\`node scripts/football/audit-academy-world.mjs --years 20 --out artifacts/academy-world/world-final\`\n\n\`node scripts/football/audit-academy-world.mjs --career-years 3 --out artifacts/academy-world/career-final\`\n\n基线源码已保存于[完整源码归档](../../artifacts/academy-world/baseline-source.json.gz)，42个文件逐一匹配运行启动时SHA。复现旧基线应在独立目录还原sources并建立type为module的package.json，再执行上述命令；直接运行当前生产源码会包含门将保护修复，不能期待与旧基线逐字一致。\n\n已有目录应加 \`--resume\`，只有完全相同源码和参数才允许续跑；从零复现应指定新目录。最终两组源码指纹均为 \`${world.sourceHash}\`。${differingFiles.length?`生成报告时工作区另有变更：${differingFiles.join('、')}，本报告保留启动版本指纹。`:'生成报告时所有被记录源码仍与运行版本一致。'}\n\n## 三季经理培养情景\n\n接手江桥联队。17岁球员提拔后，在健康且同位置可排阵时优先首发；16岁球员满17岁后实际外租，租期结束后归队、再选择当时可用的外租球队；15岁球员留学院，配置development轮换、一个重点培养名额与技术训练。每月有新证据才更新观察。\n\n| 路径 | 球员 | 初始年龄 | CA变化 | 成年分钟 | 青年分钟 | 最终注册 | 最终观察区间 |\n|---|---|---:|---|---:|---:|---|---|\n${careerRows}\n\n三个球员的禀赋和起始年龄不同，不能据此断言某条路径一定更好。该情景验证操作会改变真实注册、排阵、比赛和成长；它不是三季真人可用性研究。成年分钟保留引擎补时、加时与受伤离场的原始精度，表中四舍五入。\n\n## 20季分级能力\n\n![各级联赛平均首发能力](../../artifacts/academy-world/world-final/ability-trends.png)\n\n| 级别 | 初始首发CA | 最终首发CA | 变化 | 硬缺口球队 | 深度不足球队 |\n|---|---:|---:|---:|---:|---:|\n${driftRows}\n\n最明显变化：${drifts.slice(0,3).map(d=>`${d.id} ${f(d.drift)}分`).join('；')}。相邻级别均值倒挂：${tierInversions.length?tierInversions.join('；'):'未发现'}。5分是本报告用于发现大幅漂移的诊断门槛，不是预先验证过的平衡及格线。初始球队由既有生成器构造，长期青年稳态与初始世界的差异应结合下表趋势判断，不能只看最后一年。\n\n${leagueRows}\n\n## 代际更替与正式比赛机会\n\n下表按每季全部正式比赛的真实分钟计算：球员ID属于该赛季playerRegistry.players即为新生代，满21岁后仍算新生代；初始职业阵容属于原有球员。按俱乐部当季所在级别分组，含其杯赛与全球杯分钟。因此可与CA曲线一起区分原有球员退出与新生代接班，不会把21岁生日误当作退出青年培养体系。\n\n| 年份 | 星冠新生代分钟占比 | 开放体系一级 | 开放体系二级 | 开放体系三级 |\n|---|---:|---:|---:|---:|\n${generationRows}\n\n## 人员供需、首秀和退出\n\n| 年份 | 正式比赛 | 当年退役 | 历届青年现役职业 | 青年自由球员 | 当年新增职业首秀 | 首秀平均年龄 | 深度不足球队 | 存档MiB |\n|---|---:|---:|---:|---:|---:|---:|---:|---:|\n${annualRows}\n\n二十年入库青年共 ${total(world.seasons,'intake')} 人，所有球员当年退役累计 ${total(world.seasons,'retired')} 人；最后仍有 ${deferred} 名球员记录了延期退役且尚未退役。初始${firstYear}届 ${firstCohort.count} 人中，${firstCohort.everProfessional} 人在观察窗口内获得至少一次成年正式出场（${f(firstCohort.everProfessional/firstCohort.count*100,1)}%）。这是一届代表青年获得职业出场的比例，不能等同普通青少年成为职业球员的概率。\n\n最近届次没有同样长的观察窗口，不能把其较低首秀率解释为选拔变严。\n\n| 届次 | 人数 | 曾成年正式出场 | 比例 | 仍在学院 | 现役职业/外租 | 自由球员 | 已退役 |\n|---|---:|---:|---:|---:|---:|---:|---:|\n${cohortRows}\n\nU21累计正式出场 ${f(minutesAll,0)} 分钟；以下为最终赛季，人数在各级内去重，同年转级球员可能出现在两个级别。\n\n| 最终赛季级别 | U21球员 | 分钟 | 出场人次 |\n|---|---:|---:|---:|\n${under21Final}\n\n## 第13季人才定点诊断\n\n对330年末快照的独立只读诊断：10,452名新生代中，自然位置CA≥80有119人，PA≥85有824人、PA≥90有255人。119名已成材者分别在星冠4人、开放一级50人、二级33人、三级32人，即65人仍在较低级别。已满23岁且PA≥85的384人中，103人达到CA80，120人当季正式出场不足450分钟。自由球员池最高CA为74.28。\n\n这个定点结果提示还需检查培养机会和已签约球员的向上流动，不能直接把顶级联赛能力下降归因于PA尾部不足。PA为开发者审计读取的隐藏上界，未向游戏球探开放；此处CA按自然位置，和上方首发槽位均值不是同一汇总口径。见[原始定点诊断](../../artifacts/academy-world/talent-diagnosis-330.json)。\n\n### 具体的出场机会阻塞\n\n前腰马塞洛·雷斯·诺盖拉15岁初始CA58.499，在青年阶段已有108场、8,260分钟；321年6月30日提拔前最近一次周采样CA71.035，可作为提拔时水平的近似而非事件时精确值。此后一直留队，至330年末自然位置CA84.467，但已核对的真实成年赛事累计0分钟，330年末检查时健康可用。固定4-3-3没有前腰位；他在CM槽位经熟悉度调整后的选人分64.742，排名第4；对同一年度末阵容改用4-2-3-1选阵时会入选。AI没有常规轮换换人，仅受伤时换人；他又因CA较高，不满足低CA外租条件。\n\n这是选阵、轮换与外租条件造成的机会阻塞，不是分钟计数故障。下一步应优先考虑按球队现有角色选择阵型、常规轮换，以及按实际出场机会判断外租需求；不能只按自然位置总评决定谁不需要外租。见[完整球员与排阵证据](../../artifacts/academy-world/am-opportunity-330.json)。\n\n## 名额口径\n\n现有25名21岁以上/40人总名单用于阻止新增签约、提拔和租入；球员满21岁不会自动解约，正式报名与预备队没有分离。这是已有设计边界，见[青训系统实现](青训系统实现.md)。下表反映年龄增长和归队后的实际人数，不能把超过入队参考值直接记成违反现有硬规则。它说明正式年度报名流程尚未闭环，需要注册名单、预备队和球员机会的处理机制，不宜靠自动解约补洞。\n\n| 年份 | 21岁以上多于25人的球队 | 总人数多于40的球队 | 单队最多21+人数 | 单队最多总人数 |\n|---|---:|---:|---:|---:|\n${capacityRows}\n\n## 位置与可运行性\n\n总人数至少11人且自然位置GK至少1人为名单硬检查；GK至少2人、CB至少3人为深度诊断，不是引擎硬性报名规则。审计没有额外修补名单；游戏年度补位本身会尝试满足18人、2名门将，受候选和入队名额限制，不保证补足。比赛当天健康人数与门将另统计。\n\n首次出现位置深度不足的年份：${firstDepthYear?.year||'无'}。当时具体名单：${firstDepthExamples}。总人数充足不代表位置结构合理，过多边锋或前锋不会自动补齐中卫深度。\n\n最终 ${hard.length} 队存在硬缺口，${shallow.length} 队深度不足，${noBacks.length} 队至少缺少一个边后卫自然位置。全部比赛日可用性告警 ${world.availabilityWarnings.length} 条，引擎中断 ${world.engineFailures.length} 次。**零中断只说明赛程能运行，不能代替人才质量、位置结构和培养乐趣的评估。**\n\n其中可用人数不足11人 ${belowEleven.length} 次；可用自然门将为0有 ${noHealthyGoalkeeper.length} 次。${warningExamples?`示例：${warningExamples}。`:""}当前引擎允许外场球员客串门将，未必因此中止比赛；名单里的门将数量与比赛当天伤停覆盖需要分别判断。\n\n首个门将告警已做历史回溯：330年6月30日，sichuan-league-club-17把第二门将鲍里斯·瓦西里耶夫外租，仅留阿克拉姆·阿舒尔；阿舒尔在8月2日第30分钟受伤16天，覆盖8月9日与13日两场。这里是单门将配置遇到伤病，不是两名门将同时受伤。证据见[历史注册与伤病事件](../../artifacts/academy-world/goalkeeper-warning-diagnosis.json)。\n\n逐条以当时注册历史与真实伤病事件复核后的分类如下；“同时存在外租GK”不等同每次都由本次外租直接造成。sichuan-league-2-club-1的6条告警发生在原门将331年末退役之后，属于退役后的补位和名额问题，不能据门将外租修复就认定已解决。该队331年末已有40人、其中21岁以上35人；不考虑名额时有154名门将候选，包括本队学院年龄接近18岁、19岁的两人（均已满17岁），但全部被入队名额阻挡。332年末41人、成年37人，152名候选仍全部无名额。这是年末最终快照筛选证据，不是退役瞬间的逐操作回放，见[退役后门将补位约束](../../artifacts/academy-world/retirement-goalkeeper-seat-diagnosis.json)。完整告警证据见[全部告警回溯](../../artifacts/academy-world/availability-diagnosis.json)。\n\n| 有证据支持的当日情况 | 告警次数 |\n|---|---:|\n${warningRows}\n\n| 自然位置 | 最终注册人数 | 该位置零人的球队数 |\n|---|---:|---:|\n${positionRows}\n\n${regressionSection}\n## 证据与性能保真\n\n- [20季完整统计](../../artifacts/academy-world/world-final/report.json)、[20季摘要](../../artifacts/academy-world/world-final/report.md)。\n- [三季经理完整统计与操作](../../artifacts/academy-world/career-final/report.json)、[经理摘要](../../artifacts/academy-world/career-final/report.md)。\n- 两个final目录均有每年完整gzip快照及最新可恢复检查点。长期状态均沿用生产保存结构，没有清空旧球员来缩小文件。\n- [整季性能优化保真](../../artifacts/academy-world/parity-optimized/parity.json)：优化前后首年完整赛季存档均为${parity.oldBytes.toLocaleString('en-US')}字节，SHA-256均为 \`${parity.oldHash}\`，包含赛果、成长、伤停与注册。\n- 原world/career与prechange目录为性能或政策基线，不能替代最终final目录。性能优化后两组最终实验都从头运行，没有跨版本拼接年份。\n\n## 存档体积与执行成本\n\n第15季完整存档为177.7 MiB，其中成长记录103.0 MiB、球员注册58.3 MiB、当前赛季赛程14.8 MiB。主要体积来源是成长与注册状态，不能只归因于比赛历史。第20季实际体积见年度表；gzip年度快照是审计归档，并不代表浏览器存档已经采用分块或冷数据归档。见[第15季体积分解](../../artifacts/academy-world/save-size-332.json)。后续应评估冷热档案分离和增量保存；本报告没有测量低内存设备上的完整交互延迟。\n\n## 后续优先事项与边界\n\n优先处理按角色选阵、常规轮换与按实际机会外租，再检查已签约人才向上流动；正式报名/预备队及冷热档案分离需要独立设计。修复后仍应按相同口径复测多年，而不是只验证两场门将问题消失。\n\n当前报告验证的是生产系统在这些固定条件下的结果。学院背景球员没有独立可签约职业档案；教练、预算、设施扩建、合同和转会市场还不是完整经济模型。体格成长不直接给比赛属性加分。未通过长期平衡时，应按分级能力、入选率与位置缺口定位原因，再用相同口径复测，不能单靠提高全体PA掩盖供需问题。\n`;
const file=path.join(root,'docs/football/青训长期验证.md');fs.writeFileSync(file,doc);
const analysis={availabilityDiagnosis:diagnosis.counts,sourceHash:world.sourceHash,finalYear,worldMatches:world.matches,careerMatches:career.matches,coachedMatches:career.coachedMatches,conclusion,severeDrift,tierInversions,hardShortage:hard.map(t=>t.id),depthShortage:shallow.map(t=>t.id),missingFullbacks:noBacks.map(t=>t.id),firstCohortProfessionalFraction:firstCohort.everProfessional/firstCohort.count,retirementDeferred:deferred,noHealthyGoalkeeper:world.availabilityWarnings.filter(w=>w.goalkeepers===0),belowEleven:world.availabilityWarnings.filter(w=>w.available<11),finalSaveMiB:last.saveBytes/1048576,capacityByYear,differingSourceFiles:differingFiles,goalkeeperRegression:regression?{status:regression.status,matches:regression.matches,sourceHash:regression.sourceHash,parent:regression.parent,warnings:regression.availabilityWarnings.length}:null};
fs.writeFileSync(path.join(root,'artifacts/academy-world/analysis.json'),JSON.stringify(analysis,null,2)+'\n');
console.log(JSON.stringify({document:file,worldMatches:world.matches,careerMatches:career.matches,conclusion,depthShortage:shallow.length,finalSaveMiB:analysis.finalSaveMiB},null,2));
